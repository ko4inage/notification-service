services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=pgConsumer'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5445:5432'
